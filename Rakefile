# Not used for now (Heroku Scheduler Addons don't support more than 30s jobs)
namespace :backup do
  task :sublimevideo_mongohq do
    `bundle exec backup perform -t sublimevideo_mongohq -c config.rb`
  end
end